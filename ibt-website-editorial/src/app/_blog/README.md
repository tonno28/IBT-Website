# Blog & Ratgeber — vorübergehend ausgeblendet

Der Ordner heißt `_blog` statt `blog`. Der Unterstrich macht ihn im Next.js
App Router zu einem privaten Ordner: Die Seite wird nicht mehr gebaut, ist
unter /blog nicht erreichbar und taucht in keiner Navigation auf.

Wieder einblenden: Ordner zurück in `blog` umbenennen und den Link im
Footer (Abschnitt "Tools") wieder ergänzen.
